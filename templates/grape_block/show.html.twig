{% extends "baseAdmin.html.twig" %}

{% block body %}
    <h1>GrapeBlock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grape_block.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ grape_block.label }}</td>
            </tr>
            <tr>
                <th>Attributes</th>
                <td>{{ grape_block.attributes }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ grape_block.name }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ grape_block.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grape_block_index') }}">back to list</a>

    <a href="{{ path('grape_block_edit', {'id': grape_block.id}) }}">edit</a>

    {{ include('grape_block/_delete_form.html.twig') }}
{% endblock %}

{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Menu creation</h1>
    <div class="col-4">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-success" />
    {{ form_end(form) }}
    </div>

    <ul>
        <li>
            <a href="{{ path('myadmin_menu_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}

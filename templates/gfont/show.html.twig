{% extends 'baseAdmin.html.twig' %}

{% block title %}Gfont {% endblock %}

{% block body %}
    <h1>Gfont</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gfont.id }}</td>
            </tr>
            <tr>
                <th>FamilyName</th>
                <td>{{ gfont.familyName }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gfont.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ gfont.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gfont_index') }}">back to list</a>

    <a href="{{ path('gfont_edit', {'id': gfont.id}) }}">edit</a>

    {{ include('gfont/_delete_form.html.twig') }}
{% endblock %}
